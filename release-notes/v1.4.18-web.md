## 中文

- 放宽管理 API 的路由参数长度，兼容历史超长书签 slug 的读取和改名。
- 优化书签与分组显示名称，提升按场景搜索时的可识别性；不改变 URL、归属、重复放置或排序。

## English

- Increased the management API route parameter limit so legacy long bookmark slugs remain readable and editable.
- Refined bookmark and group display names for clearer scene-aware search without changing URLs, placements, duplicate references, or ordering.
