## 中文

- 搜索书签时临时展开包含匹配结果的分组，清空搜索后恢复搜索前的折叠状态。
- 发布 HarborDeck 1.4.6，多架构 Docker 镜像和浏览器扩展同步构建。

## English

- Matching bookmark groups now expand temporarily while searching and restore their previous collapsed state when the query is cleared.
- Published HarborDeck 1.4.6 with the multi-architecture Docker image and browser extension build.
