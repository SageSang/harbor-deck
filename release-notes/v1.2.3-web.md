## 中文

- 增加独立的内外网健康检查地址，并自动补齐 `/api/health` 路径。
- 简化书签表单，中文书签名称自动生成更稳定的 slug。

## English

- Added independent LAN/WAN health-check addresses with the `/api/health` path appended automatically.
- Simplified bookmark forms and generated more stable slugs for Chinese bookmark names.
